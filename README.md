# Workout Lab 100

Local fitness ledger (desktop) and on-device movement tracker (mobile).

Track (planned): session-based recording, map with optional speed gradient,
travel vs workout sessions, custom day windows, no ads, data stays on device
until the user exports.